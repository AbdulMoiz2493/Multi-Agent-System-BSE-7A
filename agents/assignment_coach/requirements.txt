# Assignment Coach Agent - Minimal Requirements
# Core web framework
fastapi
uvicorn
pydantic

# LLM
google-generativeai

# LangGraph for workflow
langgraph>=0.0.20
langchain-core>=0.1.0

# ChromaDB for long-term memory (uses built-in ONNX embeddings)
# Note: This is heavy but required for LTM functionality
chromadb>=0.5.0

# Environment loading
python-dotenv

# HTTP client (if needed)
httpx
